<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.ComputerSelling"

        >

        <activity
            android:name=".ProfileActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>

        <activity android:name=".ProductDetailActivity" />
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".AddActivity"></activity>
        <activity android:name=".EditProductActivity"></activity>
        <activity android:name=".ProductDetailUserActivity"></activity>
        <activity android:name=".HomeAdminActivity"></activity>
        <activity android:name=".BuyHistoryActivity"></activity>
        <activity android:name=".FavoriteActivity"></activity>
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".AdminStatisticActivity" />

        <activity android:name=".CartActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>